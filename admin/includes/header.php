<?php
// Common header include for admin pages (optional fragment)
// Keep minimal since modules are HTML fragments loaded into the SPA.
if (session_status() === PHP_SESSION_NONE) session_start();
?>
